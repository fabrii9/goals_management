<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_goals_category_tree" model="ir.ui.view">
        <field name="name">goals.category.tree</field>
        <field name="model">goals.category</field>
        <field name="arch" type="xml">
            <list string="Categorías">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <record id="view_goals_category_form" model="ir.ui.view">
        <field name="name">goals.category.form</field>
        <field name="model">goals.category</field>
        <field name="arch" type="xml">
            <form string="Categoría">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                        <field name="active" invisible="1"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_goals_category" model="ir.actions.act_window">
        <field name="name">Categorías</field>
        <field name="res_model">goals.category</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
